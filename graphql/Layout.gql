query layout($name: String!, $target: String!) {
	layout(name: $name, target: $target) {
		uuid
		details {
			name
			uuid
			author {
				name
				discordTag
			}
			description
			color
			version
		}
		target
		last_updated
		baselayout
		has_pieces
		pieces {
			name
			values {
				value
				image
				uuid
				json
			}
		}
	}
}

query layoutDetails($name: String!, $target: String!) {
	layout(name: $name, target: $target) {
		uuid
		details {
			name
			uuid
			author {
				name
				discordTag
			}
			description
			color
			version
		}
		target
		last_updated
		has_pieces
	}
}

query layoutsList($target: String!) {
	layoutsList(target: $target) {
		uuid
		details {
			name
			uuid
			author {
				name
				discordTag
			}
			description
			color
			version
		}
		target
		last_updated
	}
}
